# The Machine

The whole loop that turns ~20 building-hours/week into compounding expertise. **Work = learning** —
the build ships the app; the skim builds *you*. (Visual one-pager: `machine.html`.)

## The leak it fixes
1. **No compounding** — the agent does the reps, so it banks the gains, not you.
2. **Legacy teaching** — teaches the binary, not the current model landscape. Stale on arrival.
3. **Use it or lose it** — teach what you won't use and you forget it (a physics degree untouched 30 years).

## The loop
1. **Build** — real work, agent doing the labour (FixerUpper, StockAnalytics, a local-LLM task). ~20 hrs/wk. *The ore.*
2. **Breadcrumb** — the doer self-tags its move as it works: `← constrained decoding here, to force the JSON`. Near-free; it already holds the context. → **what you do.** (You never watch the stream.)
   - **Watch** — watchers scan the curated `SOURCES.md` (RSS + page-diff via the web-change-detector). → **what's now possible.**
3. **Filter (the heart)** — a cheap local pass on the 5090: where do the two streams **diverge *and* recur**? A move you make often, done a stale way, when a sharper current one exists. Discard the rest, unread. → **the recurring gap.**
4. **Teach** — only survivors reach the expensive teacher. Gate (why — you hold the veto) → **derive** the principle (analogy, follow the logic, own it) → the **direction + the tell**. Command-altitude, never code.
5. **Bank** — two ledgers compound, anchored to the real task that summoned them:
   - **Head** (the scaffold / atlas): the **principle** — derived, understood, sticky. *why = when.* Few, transferable.
   - **Repo** (the cheat sheet / command bank): the **command** — banked, reused, never in your head.
6. **Loop** — the next build starts from a higher floor. The atlas grows. This is the compounding.

## The filter — the selection function
> **Teach the recurring gap between how you build and how the frontier now builds.**

One rule, three problems dead: **compounds** (only recurring patterns qualify) · **never legacy** (the now-possible stream refreshes daily) · **always used** (drawn from what you already do).

## The cost — you never watch the stream
Doer self-tags (near-free) + cheap local filter; the expensive teacher fires *only* on a survivor. A 20-hour week is a handful of small skims, not 20 hours of tokens.

## The rules it obeys
work = learning · bank the formula, keep the derivation · current not legacy · only what you'll use · command-altitude · curiosity + veto

## Build status
- **Built:** command bank (repo seed) · dynamic Codex teacher · command-altitude · wall→improve loop.
- **Specced:** `SOURCES.md` · `TEACHING_METHOD.md` · `DESIGN.md`.
- **Next — the first real loop:** breadcrumb capture (doer self-tags) + the filter (local, 5090) + watcher wiring (reuse the web-change-detector).
